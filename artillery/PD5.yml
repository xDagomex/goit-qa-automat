config:
  target: "https://httpbin.org/"
  phases:
    duration: 1
    arrivalRate: 1
    name:
  plugins:
    log:
      level: error

scenarios:
  name:  Test Scenario
  flow:
    get:
      url: "{{ target}}get"
